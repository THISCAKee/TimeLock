# Review Fix: Make Admin Panel uninstall functional while hiding external entry

Resolve the final review's Critical finding. In `deployment/installer/TimeLock.iss`, change `Uninstallable=no` to `Uninstallable=yes` and add `CreateUninstallRegKey=no`; keep no uninstaller shortcut. This must preserve `unins000.exe` generation while preventing Windows Apps & Features exposure. Update `TimeLockApp.Tests/ApplicationUninstallerTests.cs` so its installer regression assertion requires both `Uninstallable=yes` and `CreateUninstallRegKey=no`, and keep assertions for AdminWindow ownership and no MainWindow uninstall text. Update the approved design/plan wording where it currently says `Uninstallable=no` so it documents the corrected configuration. Update `deployment/installer/README.md` only if needed to remain accurate. Run the custom test harness and WPF build. Preserve unrelated worktree changes. Commit may fail due `.git/index.lock` permissions. Write report to `D:\TimeOut\TimeLockApp\.superpowers\sdd\2026-08-10-admin-panel-only-uninstall\review-fix-report.md`.
